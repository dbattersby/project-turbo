<%= turbo_stream.replace dom_id(@task), @task %>
<%= turbo_stream.replace "tasks-outstanding", pluralize(@total_tasks_outstanding, "task") %>
<%= turbo_stream.replace "tasks-completed", pluralize(@total_tasks_completed, "task") %>